enabled: false

replicaCount: 2

fullnameOverride: "phc-services"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80
  targetPort: 7050

ingress:
  enabled: false

autoscaling:
  enabled: false
