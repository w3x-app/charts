enabled: false

replicaCount: 2

fullnameOverride: "rsch-assets"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80
  targetPort: 8088

ingress:
  enabled: false

autoscaling:
  enabled: false
